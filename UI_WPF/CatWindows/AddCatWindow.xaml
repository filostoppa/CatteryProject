<Window x:Class="UI_WPF.AddCatWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI_WPF"
        mc:Ignorable="d"
        Title="Aggiungi Gatto" Height="550" Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Nome:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="1" x:Name="txtName" Margin="0,0,0,15"/>

        <TextBlock Grid.Row="2" Text="Razza:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="3" x:Name="txtBreed" Margin="0,0,0,15"/>

        <TextBlock Grid.Row="4" Text="Sesso:" Margin="0,0,0,5"/>
        <StackPanel Grid.Row="5" Orientation="Horizontal" Margin="0,0,0,15">
            <RadioButton x:Name="rbMale" Content="Maschio" IsChecked="True" Margin="0,0,20,0"/>
            <RadioButton x:Name="rbFemale" Content="Femmina"/>
        </StackPanel>

        <TextBlock Grid.Row="6" Text="Data di nascita:" Margin="0,0,0,5"/>
        <DatePicker Grid.Row="7" x:Name="dpBirthDate" Margin="0,0,0,15"/>

        <TextBlock Grid.Row="8" Text="Data di arrivo:" Margin="0,0,0,5"/>
        <DatePicker Grid.Row="9" x:Name="dpArrivalDate" Margin="0,0,0,15"/>

        <TextBlock Grid.Row="10" Text="Data di partenza:" Margin="0,0,0,5"/>
        <DatePicker Grid.Row="11" x:Name="dpDepartureDate" Margin="0,0,0,15"/>

        <TextBlock Grid.Row="12" Text="Descrizione:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="13" x:Name="txtDescription" TextWrapping="Wrap"
                 AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Margin="0,0,0,15"/>

        <StackPanel Grid.Row="14" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="btnSalva" Content="Salva" Width="80" Height="30"
                    Margin="0,0,10,0" Click="BtnSalva_Click"/>
            <Button x:Name="btnAnnulla" Content="Annulla" Width="80" Height="30"
                    Click="BtnAnnulla_Click"/>
        </StackPanel>
    </Grid>
</Window>